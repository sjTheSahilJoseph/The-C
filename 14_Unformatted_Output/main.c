#include <stdio.h>
#include <conio.h>

int main(int argc, char const* argv[]) {

	printf("Unformatted Output\n");

	// We have primarily 3 functions for unformatted output.
	// 1. putchar()
	// 2. putch()
	// 3. puts()

	// putchar and putch = for single character.
	// puts = for string.

	// Basically formatted output means that we can format our output.
	// For example, we can add more string + variable, etc...

	char c = 'k';
	putchar(c); // We can use this syntax

	

	return 0;
}
